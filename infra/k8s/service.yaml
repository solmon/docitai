apiVersion: v1
kind: Service
metadata:
  name: tenant-service
  namespace: docitai
  labels:
    app.kubernetes.io/name: tenant-service
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: tenant-service
